[pytest]
# The engine modules sit at the repo root, not in a package, so tests/ needs the
# root on sys.path explicitly — otherwise `from api import _merge_known` only
# resolves when pytest happens to be invoked from this directory.
pythonpath = .
testpaths = tests
