# Test-only dependencies. Kept out of requirements.txt so Railway does not
# install pytest into the production image.
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
pytest==8.3.4
